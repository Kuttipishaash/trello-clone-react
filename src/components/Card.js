import React, { Component } from 'react';

// A card component that can be added in board.
export default class Card extends Component {
  render() {
    return (
      <div>
        Card
      </div>
    );
  }
}
