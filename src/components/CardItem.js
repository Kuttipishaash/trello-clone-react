import React, { Component } from 'react'

// The item component that represents a card in the cards list for a particular board.
export default class CardItem extends Component {
  render() {
    return (
      <div>
        
      </div>
    )
  }
}
