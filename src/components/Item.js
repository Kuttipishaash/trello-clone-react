import React, { Component } from 'react'

// An item component that can be added to card components.
export default class Item extends Component {
  render() {
    return (
      <div>
        Item
      </div>
    )
  }
}
