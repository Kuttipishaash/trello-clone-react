import React, { Component } from 'react'

// A form component used to add a new Board as well as new Card.
export default class AddForm extends Component {
  render() {
    return (
      <div>
        AddForm
      </div>
    )
  }
}
